# Banco SQLite local. O caminho é relativo ao diretório prisma/.
DATABASE_URL="file:./dev.db"

# Defina um segredo longo e aleatório antes de publicar a aplicação.
JWT_SECRET="replace-with-a-long-random-secret"

# Envio real de 2FA por WhatsApp em produção (Twilio).
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM=""
